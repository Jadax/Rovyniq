# Canonical tax model

Purpose: UI- and transport-independent, evidence-linked tax data. Inputs are document extraction or explicit user entry; outputs are typed aggregates. Invariant: imported values retain evidence, and approval/submission state is immutable/auditable. See `src/index.ts`.
