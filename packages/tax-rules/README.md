# Tax rules

Purpose: reviewed versioned assessment-year values. Do not add business logic here. Inputs: official source verification; output: immutable configuration. Tests ensure bracket continuity and source review freshness.
