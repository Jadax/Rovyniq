# Rovyniq API

The API is an intentionally minimal health-only Node server while OIDC token validation and database adapters are selected. It exposes no taxpayer data and has no upload/return routes. Add a maintained OIDC/JWKS library before exposing protected routes.
