# Local development

Use Node 24+. From the repository root run `npm.cmd run test:all`. This initial foundation has no installed runtime dependencies. Do not add production taxpayer data or secrets. Before adding a rule, update the source register and tests; before adding integration, satisfy the authorised-SARS gate.
