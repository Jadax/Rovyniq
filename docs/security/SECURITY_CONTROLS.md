# Security controls baseline

TLS in transit; envelope encryption at rest; short-lived signed download URLs; malware scan before extraction; structured logging with ID/bank/document-content redaction; secrets manager; SAST, dependency, secret and DAST scanning; backups with restore drill; audit events for data/access/state changes. Production release must add CSP, rate limits, security headers, vulnerability response and independent review.
