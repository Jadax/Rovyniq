# Document pipeline

Use native PDF text first, selective rendering/OCR only when needed, deterministic source-code dictionaries before model inference, and JSON-schema validation. Preserve originals; derivatives are separate. First extraction target is two synthetic IRP5 layouts.
