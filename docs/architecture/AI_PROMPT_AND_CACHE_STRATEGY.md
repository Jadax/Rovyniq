# AI prompt and cache strategy

Prompts must be versioned, minimal and free of unnecessary identity data. Cache only safe derived classification/extraction artifacts keyed by document hash/model/prompt version; never cache credentials. Treat extracted text as hostile instructions.
