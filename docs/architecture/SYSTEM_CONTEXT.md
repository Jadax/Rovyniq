# System context

Taxpayer and authorised contributors provide documents and answers. The web app calls the API, which coordinates encrypted storage and background extraction. Extraction produces evidence-linked candidates; the canonical model persists reviewed data; the deterministic engine evaluates reviewed inputs; manual SARS handoff presents a capture guide. No live SARS connection is in scope.
