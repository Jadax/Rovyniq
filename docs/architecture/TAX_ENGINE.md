# Tax engine

The engine is pure and deterministic. It selects a tax-year config, produces an input/rule/output trace, and keeps calculations separate from extraction/UI. Current scope is a preliminary employment estimate only; it excludes 3699 and 4497 from ordinary employment-income summation.
